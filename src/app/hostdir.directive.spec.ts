import { HostdirDirective } from './hostdir.directive';

describe('HostdirDirective', () => {
  it('should create an instance', () => {
    const directive = new HostdirDirective();
    expect(directive).toBeTruthy();
  });
});
